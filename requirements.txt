# Direct dependencies for LLM_final_version.
#
# The working environment is the existing conda env `qdrug`
# (/opt/anaconda3/envs/qdrug, Python 3.11.14) -- the notebooks already point
# at its Jupyter kernel, so use it rather than building a new one.
# See IMPROVEMENT_PLAN.md §3.1.
#
#     conda activate qdrug
#     pip install -r requirements.txt
#
# Versions below are what `qdrug` actually had on 2026-08-04, verified by
# importing every module and running the full pipeline end to end
# (generate -> 2-fold CV -> held-out test -> latents -> complexity -> pickle).
#
# NOTE: `qdrug` was missing umap-learn and numba entirely, so
# Model_analysis.py was silently falling back to PCA for every latent plot
# (UMAP_AVAILABLE = False).  Installing umap-learn fixed that; it was purely
# additive and downgraded nothing.  numba is pinned because it is what gates
# numpy 2.x compatibility -- older numba raises
# "Numba needs NumPy 1.26 or less", which is what broke the `base` env.
#
# For byte-exact reproduction of the whole env see requirements-lock.txt.

torch==2.12.1
lightning==2.6.5

numpy==2.4.2
scipy==1.17.1
scikit-learn==1.8.0
umap-learn==0.5.12
numba==0.66.0          # transitively required by umap; pinned because it gates numpy 2.x

matplotlib==3.10.8
pandas==3.0.1          # used by lightning's CSVLogger

# Test suite (pytest tests/) added in Phase 1/2: regression tests for the
# closed forms and the loss path, including the B3 silent-NaN case.
pytest==9.1.1
